# Continuum Core prototype — Phase 1
# Core engine (continuum_core.py) needs only the Python standard library.
# These are for the MCP server and the token-budget harness.
mcp>=2.0.0          # server.py — MCP stdio server (the §07 agent-facing surface); uses MCPServer (2.x)
tiktoken>=0.7.0     # token_budget.py — real tokenizer for §08 validation
jsonschema>=4.20.0  # token_budget.py --validate — seed graph vs. locked schemas
